{
  "dependencies": {
    "express": "*",
    "http-server": "^14.1.1",
	"open-cli": "7.2.0"
  },
 "scripts": {
  "build": "webpack --mode production",
  "build:azure": "webpack --mode production && cp -r dist/* ../api/wwwroot"
}
  "name": "weather",
  "version": "1.0.0",
  "main": "app.js",
  "author": "Ravindra",
  "license": "MIT",
  "description": "Weather FrontEnd Running in Docker Container"
}
